# Gitignore for SETGuru Hybrid Application

# ----------------------------------------------------
# 1. Environment Variables (CRITICAL for Security)
# ----------------------------------------------------
# Never commit files containing sensitive API keys, passwords, etc.
# Docker Compose loads the root .env file automatically.
.env
# Individual service .env files (if you use them for local dev outside Docker Compose)
frontend/.env
backend/.env
python-ai-service/.env

# ----------------------------------------------------
# 2. Node.js Specific Ignores (Frontend & Backend)
# ----------------------------------------------------
# Node modules
**/node_modules/
# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# Runtime temporary files
/tmp/

# Next.js build output (frontend)
# The .next folder is generated by `npm run build` and copied into the Docker image.
# It should NOT be committed to Git.
frontend/.next/
# Next.js export output (if using `next export`)
frontend/out/

# Production dependencies (often generated by CI/CD)
**/dist/
**/build/

# Package manager lock files (optional, but sometimes ignored if using different lock files)
# package-lock.json (usually committed)
# yarn.lock (usually committed)

# ----------------------------------------------------
# 3. Python Specific Ignores (AI Service)
# ----------------------------------------------------
# Python virtual environment directories
**/venv/
.venv/
env/
# Byte-code files
**/*.pyc
__pycache__/
# Distribution / build folders
.Python/
build/
develop-eggs/
dist/
eggs/
.eggs/
parts/
sdist/
var/
*.egg-info/
.installed.cfg
# Installer logs
pip-log.txt
pip-delete-this-arg.txt
# Unit test / coverage reports
.pytest_cache/
.coverage
.coverage.*
.hypothesis/
.nox/
.tox/

# ----------------------------------------------------
# 4. Docker Specific Ignores
# ----------------------------------------------------
# Ignore Docker build contexts/volumes that are not source code
# db_data is the named volume for PostgreSQL data
db_data/
# Docker build cache directories
.dockerignore
# Docker related files (if generated or temporary)
docker-compose.override.yml # If you use overrides for local specific settings

# ----------------------------------------------------
# 5. IDE and OS Specific Files
# ----------------------------------------------------
# macOS
.DS_Store
.localized
# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
# VS Code
.vscode/
# IntelliJ / WebStorm
.idea/
# Eclipse
.project
.classpath
.settings/
# Sublime Text
*.sublime-project
*.sublime-workspace
# Vim
*.swp
*.bak
*.swo